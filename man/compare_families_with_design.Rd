% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_selection_core.R
\name{compare_families_with_design}
\alias{compare_families_with_design}
\title{Compare GAMLSS families for a single feature (with design matrix)}
\usage{
compare_families_with_design(
  feature_vec,
  design_df,
  families,
  criterion = "GAIC",
  gaic_k = NULL,
  min_n = 5,
  bd_vec = NULL,
  transform_mode = "strict",
  extract_coef_fn = NULL,
  formula_mu = NULL,
  metadata_df = NULL
)
}
\arguments{
\item{feature_vec}{Numeric vector with feature values across samples.}

\item{design_df}{Data frame with covariates (cleaned, no intercept column).}

\item{families}{Character vector of eligible family names.}

\item{criterion}{Character IC criterion.}

\item{gaic_k}{Optional GAIC penalty.}

\item{min_n}{Integer minimum valid observations.}

\item{bd_vec}{Optional binomial denominator vector.}

\item{transform_mode}{Character: "strict" or "safe" transformation mode.}

\item{formula_mu}{Optional model formula for direct GAMLSS fitting
(e.g., `y ~ condition + random(subject)`). When provided, covariates are
taken from `metadata_df` and `design_df` is ignored for model fitting.}

\item{metadata_df}{Optional metadata data.frame used with `formula_mu`.}
}
\value{
List with comparisons tibble.
}
\description{
Similar to `compare_families_on_feature()` but fits models with covariates
using a design matrix. Returns comparison results with coefficient tables.
}
\keyword{internal}
