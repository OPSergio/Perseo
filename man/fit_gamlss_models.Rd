% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_gamlss_models.R
\name{fit_gamlss_models}
\alias{fit_gamlss_models}
\title{Fit GAMLSS models per feature (family selection with common mask + Jacobian)}
\usage{
fit_gamlss_models(
  counts_matrix,
  design_matrix,
  candidate_families,
  criterion = c("GAIC", "BIC", "AIC"),
  gaic_k = NULL,
  min_n = 5,
  contrast_matrix = NULL,
  p_adjust = "BH",
  workers = 1,
  show_progress = TRUE,
  progress_label = "Fitting features"
)
}
\arguments{
\item{counts_matrix}{numeric matrix (features x samples).}

\item{design_matrix}{data.frame or matrix of covariates with
`nrow = ncol(counts_matrix)`.}

\item{candidate_families}{character vector of GAMLSS families to test
(e.g. `c("PO","NBI","GA","GG","IG","LOGNO","NO","TF")`).}

\item{criterion}{one of `"GAIC"`, `"BIC"`, or `"AIC"`; default `"GAIC"`.}

\item{gaic_k}{numeric penalty used when `criterion = "GAIC"`. If `NULL`,
uses `log(n_valid_obs)`.}

\item{min_n}{integer minimum number of valid observations after applying the
common mask; features below this threshold are skipped. Default `5`.}

\item{contrast_matrix}{ignored; included for future API compatibility.}

\item{p_adjust}{method passed to `p.adjust()` to compute FDR per term across
features (default `"BH"`).}

\item{workers}{integer number of parallel workers. If `> 1`, caller should
configure `future::plan()` before calling. Default `1`.}

\item{show_progress}{logical; show a `progressr` progress bar. Default `TRUE`.}

\item{progress_label}{character label shown next to the progress bar.}
}
\value{
A list with:
  \describe{
    \item{results}{tibble with columns: `feature`, `term`, `effect`, `se`,
      `stat`, `pval`, `padj`.}
    \item{selection}{tibble with columns: `feature`, `best_family`,
      `n_valid_obs`, `ic_value` (Jacobian-corrected IC).}
  }
}
\description{
Compares candidate GAMLSS families per feature on the same set of observations
(common mask), using strict, family-specific transformations and Jacobian
correction for fair IC comparison. Selects the best family by GAIC/BIC/AIC and
returns per-term statistics from `summary(fit, what = "mu")`.
}
\details{
Family comparison uses strict transforms, a common mask across
families, and Jacobian correction so ICs are comparable on the original data
scale. Coefficients/p-values are taken from `summary(., what = "mu")`.
}
\examples{
\dontrun{
fit <- fit_gamlss_models(
  counts_matrix, design, c("NBI","GG","LOGNO"),
  criterion = "BIC", min_n = 20,
  workers = 4,
  show_progress = TRUE
)
}
}
\seealso{
find_families, transform_for_family_strict
}
