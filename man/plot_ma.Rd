% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_perseo.R
\name{plot_ma}
\alias{plot_ma}
\title{MA plot for PERSEO differential expression results}
\usage{
plot_ma(
  x,
  contrast = NULL,
  counts_matrix = NULL,
  fdr_threshold = 0.05,
  label_top = 10,
  point_size = 1.8,
  alpha = 0.7,
  title = NULL
)
}
\arguments{
\item{x}{Output of \code{fit_gamlss_models()}, \code{run_perseo()}, or a
contrasts tibble. For the x-axis to show true mean expression, \code{x}
should be the full \code{run_perseo()} result or you can supply
\code{counts_matrix} directly.}

\item{contrast}{Character string naming which contrast to plot.}

\item{counts_matrix}{Optional numeric matrix (features \eqn{\times} samples)
used to compute per-feature mean expression for the x-axis. If NULL,
the feature rank is used instead.}

\item{fdr_threshold}{Numeric FDR threshold for colouring (default: 0.05).}

\item{label_top}{Integer, number of top features to label (default: 10).}

\item{point_size}{Numeric, base point size (default: 1.8).}

\item{alpha}{Numeric, point transparency (default: 0.7).}

\item{title}{Character plot title; if NULL a default is constructed.}
}
\value{
A \code{ggplot} object.
}
\description{
Displays mean expression (log2 scale, derived from the counts matrix when
available, otherwise from the contrast estimate rank) against log fold change.
Points are coloured by significance.
}
\examples{
\dontrun{
result <- run_perseo(counts, "~ group", metadata = meta,
                     contrast_variable = "group", seed = 42)
plot_ma(result, contrast = "B_vs_A", counts_matrix = counts)
}

}
