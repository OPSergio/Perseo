% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_perseo.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano plot for PERSEO differential expression results}
\usage{
plot_volcano(
  x,
  contrast = NULL,
  fdr_threshold = 0.05,
  lfc_threshold = 1,
  label_top = 10,
  point_size = 1.8,
  alpha = 0.7,
  title = NULL
)
}
\arguments{
\item{x}{Output of \code{fit_gamlss_models()}, \code{run_perseo()}, or a
contrasts tibble (must contain columns \code{contrast}, \code{estimate},
\code{p_adj}, and \code{feature}).}

\item{contrast}{Character string naming which contrast to plot. If the data
contains only one contrast it is selected automatically.}

\item{fdr_threshold}{Numeric FDR threshold (default: 0.05).}

\item{lfc_threshold}{Numeric |LFC| threshold for the "LFC only" / "Sig & LFC"
categories (default: 1, i.e. 2-fold on the log scale).}

\item{label_top}{Integer, number of top significant features to label
(default: 10). Set to 0 to suppress labels. Requires \pkg{ggrepel}.}

\item{point_size}{Numeric, base point size (default: 1.8).}

\item{alpha}{Numeric, point transparency (default: 0.7).}

\item{title}{Character plot title; if NULL a default is constructed.}
}
\value{
A \code{ggplot} object.
}
\description{
Displays log fold change (estimate) against -log10 adjusted p-value for
pairwise contrasts computed by \code{fit_gamlss_models()} or
\code{run_perseo()}. Points are coloured by significance category.
}
\examples{
\dontrun{
result <- fit_gamlss_models(counts, design, candidate_families = c("PO"),
                            contrast_matrix = C, show_progress = FALSE)
plot_volcano(result, contrast = "B_vs_A")
}

}
